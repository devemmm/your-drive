# =============================================================================
# Root .env - used only by the root docker-compose.yml (the caddy reverse
# proxy). client/ and server/ each have their own .env - see
# client/.env.example and server/.env.example.
# =============================================================================
DOMAIN=yourdrive.rw
